# MMV — Project Conventions

> **What this is.** MMV-specific rules for Claude Code. The file `CLAUDE.md` at this repo root is auto-generated by Kanban Pro and only documents ticket conventions — this file documents the game project itself.
> **Read at:** `Start session` (already in SOP.md).
> **Edit when:** an MMV convention changes. Won't be overwritten by Kanban Pro.

---

## Project at a glance
**MMV (Many More Versions of You)** — narrative life sim, 1983 college dorm setting, time-traveler protagonist, target audience adults 55+. Engine is Next.js 16 / React 19 / TypeScript / Supabase / Tailwind / Vitest. Migrations are the source of truth for storylet content.

## File roles (canonical — no duplicates)

| File | Role | Updated by |
|------|------|-----------|
| `HANDOFF.md` | Current-state pointer (1 page) | Code at end-of-session |
| `HANDOFF-archive.md` | Historical session log (append-only, do not read unless asked) | Code at end-of-session |
| `SOP.md` | Process — how to work | PM when process changes |
| `GLOSSARY.md` | Vocabulary lock | PM when terms drift |
| `CLAUDE.md` | **Auto-generated by Kanban Pro** — do not edit | Kanban Pro on project load |
| `MMV-CONVENTIONS.md` | This file — MMV-specific rules | Code or PM when conventions change |
| `docs/CONTENT-RULES.md` | Engine placement rules (chain vs pool, the 10-rule spec) | Code when engine changes |
| `docs/CONTENT-PROCESS.md` | Content creation workflow | PM when workflow changes |
| `docs/DECISIONS.md` | Design rationale log — the "why" | Code or PM after decisions |
| `docs/CHAIN-MAP.md` | Current chain wiring + flags | Code after content changes |
| Kanban `MAPPING.md` | Ticket index (read-first triage) | Kanban Pro auto-generates |
| MemPalace | Decision memory — non-obvious rationale | Code or PM |

### What does NOT go where
- Kanban does not store design rationale → DECISIONS.md or MemPalace
- MemPalace does not track task state → Kanban
- HANDOFF.md does not store process instructions → SOP.md
- HANDOFF.md does not store historical commits → HANDOFF-archive.md
- TASKS.md is deprecated — use Kanban tickets

## Critical invariants
1. **Migrations = truth.** Storylet content lives in `supabase/migrations/`. Don't edit DB directly except in dev-reset flows; codify changes as a new timestamped migration.
2. **Close tickets on evidence.** Before writing `status: col_done`, paste verifying shell output (test result, SQL scan, file listing). Ticket body = report of what happened, not a summary of what should have happened.
3. **HANDOFF refresh discipline.** Code updates HANDOFF "Branches in flight" or appends a one-liner per meaningful commit before EOD; or includes `git log --since=<last-handoff>` snippet at top of next session opener. (DECISIONS.md 2026-04-29)
4. **Don't run iCloud-synced.** Repo + Kanban must live under `~/Projects/`, never `~/Documents/`. iCloud silently corrupts `.git/`. (DECISIONS.md 2026-04-28 PATH B)
5. **`modifiedBy: claude-code`** on every Kanban ticket edit — never use Monty's profile name.
6. **Rewrite entire ticket files** on edit — Chokidar watches for partial writes.

## Environment routing

| I want to... | Go to | Why |
|--------------|-------|-----|
| Decide what to build next | claude.ai | PM has board context + memory |
| Write a storylet brief | claude.ai | Needs collision awareness, story context |
| Write code / SQL migrations | Claude Code | Needs repo, types, test runner |
| Plan a stream map or arc | claude.ai → Cowork | Plan in PM, spec in Obsidian |
| Playtest in browser | You (manually) | Code can't run browser tests reliably |
| Update design docs in Obsidian | Cowork | File access to the vault |
| Review board state / sprint plan | claude.ai | PM reads MAPPING.md |
| Debug a broken engine behavior | Claude Code | Needs codebase + test runner |
| "This doesn't feel right" | claude.ai → Code | Diagnose here, fix there |

## The content loop
```
PLAN (claude.ai)  →  SPEC (claude.ai or Cowork)  →  BUILD (Code)  →  LOOK (play it)  →  DECIDE (claude.ai)
```
Most content cycles 2–3 times. Detail in `docs/CONTENT-PROCESS.md`.

## Content Studio v2 CSS approach

The studio CSS lives at `src/app/(admin)/studio/studio.css`. Key rules:

- **Token scope**: `--bg`, `--ink`, `--line`, etc. are defined on `.studio-root`, not `:root`. All components inside `StudioShell` inherit them via cascade.
- **No `@import url()`**: Fonts (JetBrains Mono, Source Serif 4) are loaded by `next/font/google` in `content/layout.tsx` as `--font-jetbrains-mono` and `--font-source-serif`. Reference them as `var(--font-jetbrains-mono)` in CSS; the variables are available throughout `.studio-root`.
- **Per-track colors**: `--track-ink`, `--track-soft`, `--track-line`, `--track-chip` are set inline via `trackStyle(key: TrackKey)` from `src/lib/trackPalette.ts`. Never hardcode track colors in JSX — always call `trackStyle()`.
- **Tailwind for structure; CSS classes for token-heavy components**: Use Tailwind (`flex`, `grid`, `overflow-hidden`) for structural layout. Use `.sl-card`, `.track-chip`, `.stance-bar`, `.tabbar`, `.topbar`, etc. CSS classes for components that consume palette tokens.

---

## Drift checks (run when something feels off)
- **Audit closed tickets vs disk**: `npx tsx scripts/audit-closed-tickets.ts` — exits 1 if any `col_done` ticket references a missing file.
- **Build state**: `npx tsc --noEmit && npx vitest run && npm run playthrough:all` should all be green.
- **Migration order**: `ls supabase/migrations/ | sort -c` — fails on out-of-order timestamps.
